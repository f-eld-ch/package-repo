name: Update Package Repository

on:
  push:
    branches: [main]
  repository_dispatch:
    types: [new-release]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to add (e.g. v1.2.3). Leave empty to rebuild entire channel.'
        required: false
        default: ''
      channel:
        description: 'Channel to publish to.'
        required: false
        default: 'stable'
        type: choice
        options:
          - stable
          - testing
      keep_versions:
        description: 'Number of releases to keep (0 = keep all).'
        required: false
        default: '0'

jobs:
  update-repo:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: gh-pages

      - name: Fetch main branch (scripts + static files)
        run: |
          git fetch origin main
          mkdir -p "${{ runner.temp }}/main-branch"
          git archive origin/main | tar -x -C "${{ runner.temp }}/main-branch/"

      - name: Install tools
        run: |
          sudo apt-get update -q
          sudo apt-get install -y --no-install-recommends \
            dpkg-dev apt-utils createrepo-c rpm gpg

      - name: Install Hugo
        uses: peaceiris/actions-hugo@f2acb7c5f695a87535fa8eb9040475c84c4f6909 # v3.2.1
        with:
          hugo-version: 'latest'

      - name: Import signing key
        env:
          REPO_SIGNING_KEY: ${{ secrets.REPO_SIGNING_KEY }}
        run: |
          echo "$REPO_SIGNING_KEY" | gpg --batch --import
          KEY_FP=$(gpg --list-keys --with-colons packages@sitrep.ch \
            | awk -F: '/^fpr/{print $10; exit}')
          echo "${KEY_FP}:6:" | gpg --import-ownertrust

      - name: Resolve parameters
        id: params
        run: |
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            TAG="${{ github.event.client_payload.tag }}"
            CHANNEL="${{ github.event.client_payload.channel }}"
            MODE="single"
          elif [ -n "${{ github.event.inputs.tag }}" ]; then
            TAG="${{ github.event.inputs.tag }}"
            CHANNEL="${{ github.event.inputs.channel }}"
            MODE="single"
          else
            TAG=""
            CHANNEL="all"
            MODE="rebuild-all"
          fi
          echo "tag=${TAG}"         >> $GITHUB_OUTPUT
          echo "channel=${CHANNEL}" >> $GITHUB_OUTPUT
          echo "mode=${MODE}"       >> $GITHUB_OUTPUT
          echo "keep=${{ github.event.inputs.keep_versions || '0' }}" >> $GITHUB_OUTPUT

      - name: Download packages
        env:
          GH_TOKEN: ${{ github.token }}
          TAG:      ${{ steps.params.outputs.tag }}
          CHANNEL:  ${{ steps.params.outputs.channel }}
          MODE:     ${{ steps.params.outputs.mode }}
        run: |
          CHANNELS="${CHANNEL}"
          [ "${CHANNEL}" = "all" ] && CHANNELS="stable testing"
          for CH in $CHANNELS; do
            CHANNEL="$CH" bash "${{ runner.temp }}/main-branch/scripts/download-packages.sh"
          done

      - name: Prune old versions
        env:
          GH_TOKEN:      ${{ github.token }}
          CHANNEL:       ${{ steps.params.outputs.channel }}
          KEEP_VERSIONS: ${{ steps.params.outputs.keep }}
        run: |
          CHANNELS="${CHANNEL}"
          [ "${CHANNEL}" = "all" ] && CHANNELS="stable testing"
          for CH in $CHANNELS; do
            CHANNEL="$CH" bash "${{ runner.temp }}/main-branch/scripts/prune-packages.sh"
          done

      - name: Place packages into pool
        env:
          CHANNEL: ${{ steps.params.outputs.channel }}
        run: |
          CHANNELS="${CHANNEL}"
          [ "${CHANNEL}" = "all" ] && CHANNELS="stable testing"
          for CH in $CHANNELS; do
            CHANNEL="$CH" bash "${{ runner.temp }}/main-branch/scripts/place-packages.sh"
          done

      - name: Sign RPM packages
        env:
          CHANNEL: ${{ steps.params.outputs.channel }}
        run: |
          CHANNELS="${CHANNEL}"
          [ "${CHANNEL}" = "all" ] && CHANNELS="stable testing"
          for CH in $CHANNELS; do
            CHANNEL="$CH" bash "${{ runner.temp }}/main-branch/scripts/sign-rpms.sh"
          done

      - name: Generate APT metadata
        env:
          CHANNEL: ${{ steps.params.outputs.channel }}
        run: |
          CHANNELS="${CHANNEL}"
          [ "${CHANNEL}" = "all" ] && CHANNELS="stable testing"
          for CH in $CHANNELS; do
            CHANNEL="$CH" bash "${{ runner.temp }}/main-branch/scripts/build-apt.sh"
          done

      - name: Generate RPM metadata
        env:
          CHANNEL: ${{ steps.params.outputs.channel }}
        run: |
          CHANNELS="${CHANNEL}"
          [ "${CHANNEL}" = "all" ] && CHANNELS="stable testing"
          for CH in $CHANNELS; do
            CHANNEL="$CH" bash "${{ runner.temp }}/main-branch/scripts/build-rpm.sh"
          done

      - name: Generate directory indexes
        run: bash "${{ runner.temp }}/main-branch/scripts/generate-indexes.sh"

      - name: Build Hugo site
        run: hugo --source "${{ runner.temp }}/main-branch" --destination "${{ runner.temp }}/hugo-out"

      - name: Copy static files
        run: |
          cp "${{ runner.temp }}/hugo-out/index.html" ./
          cp "${{ runner.temp }}/main-branch/static-assets/sitrep-signing.asc" ./
          cp "${{ runner.temp }}/main-branch/static/sitrep.repo" rpm/
          cp "${{ runner.temp }}/main-branch/static/sitrep-testing.repo" rpm/
          cp "${{ runner.temp }}/main-branch/static/sitrep.sources" deb/
          cp "${{ runner.temp }}/main-branch/static/sitrep-testing.sources" deb/

      - name: Commit and push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          TAG="${{ steps.params.outputs.tag }}"
          CHANNEL="${{ steps.params.outputs.channel }}"
          if [ -n "${TAG}" ]; then
            MSG="chore: add ${CHANNEL} packages for ${TAG}"
          elif [ "${CHANNEL}" = "all" ]; then
            MSG="chore: rebuild all channels"
          else
            MSG="chore: rebuild ${CHANNEL} packages"
          fi
          git diff --cached --quiet || git commit -m "$MSG"
          git push
